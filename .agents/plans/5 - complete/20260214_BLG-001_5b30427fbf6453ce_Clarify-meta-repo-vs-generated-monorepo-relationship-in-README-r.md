# Clarify meta-repo vs generated monorepo relationship in README (reduce “which scaffold.py?” confusion)

Generated by `python -m usertest.cli reports export-tickets` on 2026-02-14T02:14:18Z.
- Fingerprint: `5b30427fbf6453ce`
- Source ticket: `BLG-001`

- Source ticket: `BLG-001`
- Fingerprint: `5b30427fbf6453ce`
- Export kind: `implementation`
- Stage: `triage`
- Severity: `blocker`
- Change surface kinds: `unknown`

## Title

Clarify meta-repo vs generated monorepo relationship in README (reduce “which scaffold.py?” confusion)

## Problem

The relationship between this template repo and the generated monorepo (which contains its own `tools/scaffold/scaffold.py`) is implicit and can confuse first-time users.

## User impact

Users run commands in the wrong place, misinterpret paths, and fail to reach first success.

## Proposed fix

Add a short “What this repo is / isn’t” callout near the top of `README.md`, plus a 3-step flow: (1) cookiecutter render, (2) cd into generated repo, (3) run `python tools/scaffold/scaffold.py ...`.

## Investigation steps

- Review `README.md` for an explicit callout that the root repo is a template repo and the CLI lives in the generated repo.
- Ensure links point directly to `templates/monorepo-root/{{cookiecutter.repo_slug}}/tools/scaffold/README.md` for the generated CLI usage.
- Open root `README.md` and confirm there is no single explicit render + doctor smoke path with expected output/exit code.
- Confirm the canonical quickstart render target (`templates/monorepo-root`) and the canonical success signal (`doctor` prints `OK`, exit 0) match tests/source expectations.
- Confirm the template defaults in `templates/monorepo-root/cookiecutter.json` (e.g., default `repo_slug`) so the README example is deterministic.
- Ensure the documented commands are PowerShell-friendly (paths and quoting guidance if needed).
- Validate the copy/paste quickstart on a clean environment/CI runner.
- Find the component issuing `list_directory`/stat calls for `tools/scaffold` and enumerate assumed repo layouts.
- Add a lightweight layout detector (e.g., presence of `templates/monorepo-root/.../tools/scaffold/scaffold.py` vs `tools/scaffold/scaffold.py`).
- Replace direct probing of `tools/scaffold` with resolution via the detector; on mismatch, return a guided error message.
- Add a regression test/fixture covering both template-repo and generated-repo layouts.
- Confirm the documented template smoke-check command and add a PowerShell note for literal `{{cookiecutter.*}}` paths.

## Success criteria

- `README.md` explicitly states the repo is a Cookiecutter template repo and that the scaffolder CLI is used from the generated monorepo.
- A new user can identify the correct `scaffold.py` path without reading additional docs.
- Root `README.md` contains a “Quickstart (First Success)” section with an exact, runnable command sequence to render `templates/monorepo-root` and run `doctor`.
- Following the README produces a generated repo in a clearly stated location (e.g., `.tmp/my-monorepo`) and `doctor` prints `OK` and exits with code 0.
- README includes a brief, explicit statement distinguishing template-repo vs generated-monorepo contexts (and where the generated scaffolder lives).
- Inspection/automation no longer fails with ENOENT when run against the template repo; it resolves to the template scaffold path.
- When run in the wrong context, the error message explicitly calls out template-vs-generated layout mismatch and points to the correct path.
- `README.md` includes an explicit 'Template-repo smoke check' command with expected output `OK` and a short Windows/PowerShell note.
- Automated tests cover both layouts and pass.

## Evidence breadth (counts)

- missions: 4
- targets: 1
- repo_inputs: 1
- agents: 3
- personas: 4
- runs: 11

## Evidence atom ids

- `project_scaffold/20260122T003742Z/codex/0:confidence_missing:1`
- `project_scaffold/20260122T004345Z/codex/0:suggested_change:1`
- `project_scaffold/20260122T013344Z/claude/0:agent_last_message_artifact:1`
- `project_scaffold/20260122T013344Z/claude/0:confusion_point:1`
- `project_scaffold/20260122T015942Z/gemini/0:confusion_point:2`
- `project_scaffold/20260210T063911Z/codex/0:agent_last_message_artifact:1`
- `project_scaffold/20260210T063911Z/codex/0:suggested_change:1`
- `project_scaffold/20260210T064954Z/claude/0:confusion_point:2`
- `project_scaffold/20260210T064954Z/claude/0:suggested_change:2`
- `project_scaffold/20260211T070755Z/codex/20260211:agent_last_message_artifact:1`
- `project_scaffold/20260211T071056Z/gemini/20260211:agent_stderr_artifact:1`
- `project_scaffold/20260211T071251Z/codex/20260211:agent_last_message_artifact:1`
- `project_scaffold/20260211T071617Z/gemini/20260211:agent_last_message_artifact:1`
- `project_scaffold/20260211T071905Z/codex/20260211:agent_last_message_artifact:1`
