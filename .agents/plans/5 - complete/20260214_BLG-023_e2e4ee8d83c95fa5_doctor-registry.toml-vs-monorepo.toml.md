# Scaffold docs: add glossary + clarify extension workflow (kind/generator/registry/manifest/doctor; registry.toml vs monorepo.toml)

Generated by `python -m usertest.cli reports export-tickets` on 2026-02-14T02:14:19Z.
- Fingerprint: `e2e4ee8d83c95fa5`
- Source ticket: `BLG-023`

- Source ticket: `BLG-023`
- Fingerprint: `e2e4ee8d83c95fa5`
- Export kind: `implementation`
- Stage: `triage`
- Severity: `medium`
- Change surface kinds: `unknown`

## Title

Scaffold docs: add glossary + clarify extension workflow (kind/generator/registry/manifest/doctor; registry.toml vs monorepo.toml)

## Problem

Scaffold documentation introduces core terms (kind, generator, registry, manifest, doctor) implicitly and does not clearly explain how to extend/customize generators or how `registry.toml` and `monorepo.toml` relate, leading to confusion and mis-edits.

## User impact

First-time users take longer to build a correct mental model and are more likely to edit the wrong file (registry vs manifest), increasing trial-and-error when extending or troubleshooting scaffold behavior.

## Proposed fix

Update `templates/monorepo-root/{{cookiecutter.repo_slug}}/tools/scaffold/README.md` with (1) a short Glossary section defining kind/generator/registry/manifest/doctor and pointing to the exact file/command for each, and (2) a concise section on adding/customizing generators explaining how to edit `tools/scaffold/registry.toml` and how tasks/outputs are recorded into `tools/scaffold/monorepo.toml` (including any safe workflows like dry-run/no-install if supported).

## Investigation steps

- Locate the first point in scaffold docs/CLI output where the terms appear without definition.
- Confirm current source-of-truth responsibilities for `tools/scaffold/registry.toml` vs `tools/scaffold/monorepo.toml` and the role of the `doctor` command.
- Draft concise definitions and an extension lifecycle (registry definition -> add command -> manifest updates -> run tasks) and cross-check wording against implementation/tests.

## Success criteria

- Scaffold README includes a clearly findable Glossary defining at least: kind, generator, registry, manifest, doctor.
- Each glossary entry points to the implementing file/command (e.g., `registry.toml`, `monorepo.toml`, `doctor`).
- Docs include a concrete, step-by-step extension path for adding a new generator without code archaeology.
- Fewer user errors stemming from misunderstanding registry vs manifest responsibilities.

## Evidence breadth (counts)

- missions: 1
- targets: 1
- repo_inputs: 1
- agents: 1
- personas: 1
- runs: 2

## Evidence atom ids

- `project_scaffold/20260122T003742Z/codex/0:suggested_change:2`
- `project_scaffold/20260210T063911Z/codex/0:suggested_change:2`
